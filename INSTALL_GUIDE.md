# Git Daily Summary - 설치 및 업데이트 가이드

## 최초 설치

### 1. 다운로드

GitHub에서 최신 릴리즈를 다운로드합니다.

> https://github.com/JaechanGo/git-daily-summary/releases

- **Source code (zip)** 을 클릭하여 다운로드
- 또는 릴리즈에 첨부된 ZIP 파일이 있다면 해당 파일 다운로드

### 2. 압축 해제

다운로드한 ZIP 파일을 원하는 위치에 압축 해제합니다.

```
예시: C:\Extensions\git-daily-summary
예시: ~/Extensions/git-daily-summary
```

> 이 폴더는 삭제하면 확장이 동작하지 않으므로, 고정된 위치에 보관하세요.

### 3. Chrome에 확장 프로그램 등록

1. Chrome 주소창에 `chrome://extensions` 입력
2. 우측 상단 **개발자 모드** 토글을 켭니다
3. **압축해제된 확장 프로그램을 로드합니다** 클릭
4. 압축 해제한 폴더를 선택
5. 확장 목록에 **Git Daily Summary**가 나타나면 완료

### 4. 초기 설정

1. 확장 아이콘 클릭 → 우측 상단 **⚙ 설정** 클릭
2. GitHub / Gitea 토큰 및 사용자 정보 입력
3. AI Provider 선택 및 API Key 입력
4. **저장** 클릭

---

## 업데이트 알림 확인

확장이 6시간마다 자동으로 새 버전을 확인합니다.

### 팝업에서 확인

새 버전이 있으면 확장 아이콘에 빨간 **NEW** 배지가 표시되고, 팝업 상단에 업데이트 배너가 나타납니다.

### 설정 페이지에서 확인

1. 설정 페이지 하단 **버전 정보** 섹션에서 현재 버전 확인
2. **최신 버전 확인** 버튼 클릭
3. 업데이트가 있으면 **다운로드** 버튼이 나타남

---

## 업데이트 적용

### 방법 1: 기존 폴더에 덮어쓰기 (권장)

1. **다운로드** 버튼 클릭 → ZIP 파일 다운로드
2. 다운로드한 ZIP을 압축 해제
3. 기존 확장 폴더의 **모든 파일을 삭제** (폴더 자체는 유지)
4. 새로 압축 해제한 파일들을 기존 폴더에 **복사**
5. `chrome://extensions` → Git Daily Summary 카드의 **새로고침(↻)** 아이콘 클릭

> 설정(토큰, AI Key 등)은 Chrome storage에 저장되므로 파일을 교체해도 유지됩니다.

### 방법 2: 새 폴더로 교체

1. ZIP 다운로드 후 새 위치에 압축 해제
2. `chrome://extensions` → 기존 Git Daily Summary **제거**
3. **압축해제된 확장 프로그램을 로드합니다** → 새 폴더 선택

> 이 방법도 설정은 유지됩니다. (같은 확장 ID가 유지되는 경우)

### 방법 3: git pull (개발자용)

레포를 clone한 경우 가장 간편합니다.

```bash
cd git-daily-summary
git pull origin main
```

이후 `chrome://extensions` → **새로고침(↻)** 클릭

---

## 업데이트 확인 (정상 적용 여부)

1. 확장 팝업을 열면 타이틀 옆에 버전이 표시됩니다: `Git Daily Summary v1.1.0`
2. 설정 페이지 하단 **버전 정보**에서도 현재 버전을 확인할 수 있습니다
3. **최신 버전 확인** 클릭 시 **"✅ 최신 버전입니다"** 가 나오면 정상

---

## FAQ

**Q: 업데이트하면 설정이 초기화되나요?**
A: 아닙니다. 토큰, AI Key, 선택된 레포 등 모든 설정은 Chrome storage에 저장되므로 파일을 교체해도 유지됩니다.

**Q: 다운로드 버튼을 눌렀는데 404 오류가 나요.**
A: 설정 페이지에서 **최신 버전 확인**을 다시 눌러 URL을 갱신해보세요. 그래도 안 되면 GitHub 릴리즈 페이지에서 직접 다운로드하세요.

**Q: 자동 업데이트는 안 되나요?**
A: Chrome Web Store에 등록되지 않은 확장은 자동 업데이트가 불가합니다. 알림을 통해 새 버전을 확인하고, 수동으로 파일을 교체해야 합니다.

**Q: 새로고침 후에도 버전이 안 바뀌어요.**
A: 브라우저 캐시 문제일 수 있습니다. `chrome://extensions`에서 확장을 제거한 뒤 다시 로드해보세요.
